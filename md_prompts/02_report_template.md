
---

## 🧱 Файл 2: `02_report_template.md`

Этот файл — **скелет отчёта**, который Copilot должен заполнить.  
Я сразу делаю его на русском, под зампреда/риск-менеджмент.

```markdown
---
title: "Модель вероятности дефолта (PD) для продукта ______"
author: "Команда риск-аналитики / Data Science"
date: "YYYY-MM-DD"
---

# 1. Резюме (Executive Summary)

<!-- COPILOT: кратко описать цель модели, основные модели (Application, Behavioral, Hybrid),
ключевые метрики (ROC-AUC, Gini, KS, Recall), и вывод о пригодности модели к использованию. -->

- Цель разработки модели
- Краткое описание:
  - Модель PD на выдаче (Application)
  - Поведенческая модель PD (Behavioral)
  - Гибридная модель PD (Hybrid)
- Основные показатели качества:
  - ROC-AUC / Gini
  - KS
  - Калибровка
- Вывод: **можно / нельзя** рекомендовать к внедрению, при каких условиях.

---

# 2. Бизнес-контекст и постановка задачи

<!-- COPILOT: описать продукт, бизнес-контекст, требование зампреда:
  - иметь поведенческую модель PD (90+ DPD через месяц),
  - обеспечить согласованность PD на момент выдачи с действующими скоринговыми PD,
  - получить логичную динамику PD по мере роста loan_age_months. -->

## 2.1. Продукт и портфель

- Описание продукта (тип займа, сегмент клиентов, валюта, срок и пр.).
- Краткая характеристика портфеля:
  - объём выборки,
  - период данных (например, 31.12.2021 – 30.11.2024),
  - особенности портфеля.

## 2.2. Цель моделирования

- Определение таргета:
  - дефолт = 90+ DPD,
  - прогнозный горизонт = 1 месяц.
- Необходимость поведенческого PD (Behavioral PD).
- Требование к согласованию с Application PD на момент выдачи.

---

# 3. Данные и выборка

<!-- COPILOT: собрать информацию о данных из EDA/подготовительных скриптов:
  - источники данных,
  - временное окно,
  - размер выборки,
  - фильтры,
  - train/valid/test/OOT разбиения. -->

## 3.1. Источники и период данных

- Перечень источников (DWH, скоринговая система, транзакционные данные и т.д.).
- Период наблюдений:
  - начальная дата,
  - конечная дата,
  - период формирования таргета.

## 3.2. Структура выборки

- Общее количество займов / контрактов.
- Количество наблюдений по месяцам контракта.
- Доля дефолтов (90+ DPD) в итоговой выборке.
- Разбиение на:
  - обучающую выборку,
  - валидационную,
  - тестовую,
  - OOT (если есть).

## 3.3. Основные признаки

- Описание групп признаков:
  - аппликационные признаки (до выдачи),
  - поведенческие признаки (история платежей, просрочки, лимиты и т.д.),
  - технические признаки (loan_age_months, т.п.).
- Кратко: критерии отбора признаков, обработка пропусков, категориальные переменные.

---

# 4. Исходные модели: Application PD и Behavioral PD

## 4.1. Модель на выдаче (Application PD)

<!-- COPILOT: найти код/отчёты, относящиеся к скоринговой модели на выдаче
и кратко описать:
  - тип модели,
  - использованные признаки,
  - период обучения,
  - ключевые метрики. -->

- Тип модели (логистическая регрессия / градиентный бустинг и т.д.).
- Основные показатели качества (на обучении / валидации / OOT).
- Особенности калибровки PD на выдаче.

## 4.2. Поведенческая модель (Behavioral PD)

<!-- COPILOT: описать разработанную поведенческую модель:
  - таргет (90+ DPD через месяц),
  - фичи,
  - тип модели (например, gradient boosting / XGBoost / LightGBM),
  - ROC-AUC ~ 0.92, Recall ~ 0.67,
  - особенности несбалансированности. -->

- Цель: прогноз дефолта (90+ DPD) на горизонте 1 месяц.
- Особенности таргета: сдвиг факта дефолта на 1 месяц назад.
- Обработка несбалансированности (взвешивание классов, oversampling/undersampling, если было).
- Модель:
  - алгоритм,
  - основные гиперпараметры.
- Результаты:
  - ROC-AUC, Gini, KS,
  - Recall / Precision,
  - важности признаков (top-N).

### 4.3. Графики качества Behavioral модели

<!-- COPILOT: вставить интерактивные графики Plotly:
  - ROC-кривая,
  - распределение PD_behavioral,
  - возможно: калибровочный график (predicted vs observed). -->

```html
<!-- Пример вставки Plotly-графика -->
<div id="behavioral_roc_curve">
  <!-- COPILOT: вставить сюда fig.to_html(full_html=False, include_plotlyjs=False) для ROC-кривой Behavioral модели -->
</div>
````

---

# 5. Гибридная модель (Hybrid PD)

## 5.1. Мотивация гибридной модели

<!-- COPILOT: описать, почему одной Behavioral модели недостаточно:
  - существующая скоринговая PD на выдаче,
  - требование согласовать PD на момент выдачи,
  - необходимость плавного перехода от Application к Behavioral PD. -->

## 5.2. Формула гибридной модели

* Весовая функция:

```math
w(a) = \frac{1}{1 + \exp(k \cdot (a - x_0))}
```

где:

* (a = \text{loan_age_months}),

* (k) — параметр крутизны (steepness),

* (x_0) — середина, при которой вклад Application и Behavioral моделей равен 50/50.

* Итоговая вероятность дефолта:

```math
PD_{\text{final}}(a, x) = w(a) \cdot PD_{\text{app}}(x) + (1 - w(a)) \cdot PD_{\text{beh}}(a, x)
```

## 5.3. Подбор параметров k и x0

<!-- COPILOT: описать, как подбирались k и x0:
  - оптимизация (например, через Optuna),
  - целевая функция/критерии:
    - минимизация расхождения PD_final(age=0) и PD_app,
    - монотонный (или логичный) профиль среднего PD по loan_age_months,
    - сохранение ROC-AUC / Gini. -->

* Критерии подбора:

  * RMSE/MAE между `PD_final(age=0)` и `PD_app`.
  * Штраф за немонотонность среднего `PD_final` по `loan_age_months`.
  * Контроль деградации дискриминации (ROC-AUC, Gini).

## 5.4. Поведение PD_final по loan_age_months

<!-- COPILOT: построить график среднего PD_final по возрасту займа,
сравнить его с:
  - средним PD_app (на age=0),
  - средним PD_beh по age.
Построить Plotly-график. -->

```html
<div id="pd_by_age">
  <!-- COPILOT: вставить Plotly-график:
       - линия для среднего PD_app (точка при age=0),
       - линия для среднего PD_beh(age),
       - линия для среднего PD_final(age). -->
</div>
```

Краткий текстовый анализ:

* согласованность PD_final с PD_app при age=0;
* плавность или монотонность профиля по age;
* интерпретация с точки зрения риска.

---

# 6. Качество и валидация гибридной модели

<!-- COPILOT: сравнить качества моделей:
  - Application,
  - Behavioral,
  - Hybrid. -->

## 6.1. Сравнение метрик

Таблица (пример):

| Модель         | Выборка    | ROC-AUC | Gini | KS | Recall (default=1) | Примечания                |
| -------------- | ---------- | ------- | ---- | -- | ------------------ | ------------------------- |
| Application PD | Train/OOT  |         |      |    |                    |                           |
| Behavioral PD  | Train/Test | 0.92    |      |    | 0.67               | несбалансированные данные |
| Hybrid PD      | Train/OOT  |         |      |    |                    |                           |

<!-- COPILOT: заполнить фактическими значениями. -->

## 6.2. ROC-кривые и другие графики

```html
<div id="roc_all_models">
  <!-- COPILOT: вставить интерактивный Plotly-график ROC-кривых для Application, Behavioral и Hybrid моделей. -->
</div>
```

* Анализ различий в качестве между моделями.
* Оценка стабильности Hybrid PD на OOT выборке (если есть).

## 6.3. Калибровка PD

<!-- COPILOT: описать и/или показать:
  - calibration plot для Hybrid PD,
  - насколько хорошо predicted PD соответствует observed DR в бинах. -->

---

# 7. Реализация и внедрение

<!-- COPILOT: собрать информацию из кода:
  - в каком виде модель реализована (Python-пакет, сервис, скрипты),
  - как рассчитываются PD на проде,
  - какие параметры и артефакты нужно хранить. -->

## 7.1. Архитектура реализации

* Где и как развёрнута модель:

  * batch / real-time,
  * какие входные данные требуются.
* Путь расчёта PD_final:

  * получение PD_app,
  * расчёт PD_beh,
  * применение весовой функции и объединение.

## 7.2. Версионность и артефакты

* Где хранятся:

  * веса моделей,
  * параметры k, x0,
  * код.
* Требования к мониторингу и переобучению.

---

# 8. Ограничения и рекомендации

<!-- COPILOT: описать ограничения модели:
  - исторический период (нет PD_app до 31.08.2023),
  - возможные bias,
  - чувствительность к изменениям продукта / политики.
И дать рекомендации:
  - дополнительная калибровка,
  - регулярный мониторинг,
  - тестирование на новых когортных выборках и пр. -->

---

# 9. Приложения

<!-- COPILOT: опционально:
  - вынести сюда подробные таблицы с метриками,
  - топ-важности признаков,
  - дополнительные графики,
  - примеры SQL / Python-кода. -->

* Приложение A: Дополнительные метрики
* Приложение B: Важности признаков
* Приложение C: Технические детали реализации

```

---

## 🚀 Как использовать это у себя

1. Клади эти два файла в репо:  
   - `01_copilot_instructions.md`  
   - `02_report_template.md`

2. В Copilot Chat (VS Code / GitHub):
   - открой репозиторий с моделями,
   - в первый запрос просто **скопируй содержимое `01_copilot_instructions.md`**,
   - вторым сообщением:  
     _«Пройди по репозиторию и заполни `02_report_template.md`, сохрани как `report.md` и сгенерируй `report.html` с Plotly-графиками»._

3. Дальше уже будешь итеративно докручивать:
   - «добавь ещё раздел про мониторинг»,  
   - «обнови таблицу метрик», и т.д.

Если хочешь, в следующем сообщении могу:
- расписать **пример кода на Python**, который собирает метрики и генерит Plotly-графики + HTML (без Copilot),  
- или заточить шаблон под конкретные имена файлов твоего проекта (если скинешь структуру репо).
```
